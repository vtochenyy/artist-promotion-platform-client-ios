# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'artist-promotion-platform' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for artist-promotion-platform

  # Formatter
  pod 'SwiftLint'
  pod 'SwiftFormat/CLI'

  # Initialization
  pod 'Then'

  # Constraints
  pod 'SnapKit', '~> 5.0.0'
end
